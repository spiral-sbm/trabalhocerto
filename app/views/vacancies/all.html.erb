<section class="bg-light bg-gradient">
<div class="container">
<h1 class="mb-5 display-4">Vagas</h1>
<div class="row">
<div class="col-lg-10">
<% @vacancies.each do |vacancy| %>
    <%= render partial: 'vacancy_card', locals: { vacancy: vacancy } %>
<% end %>
</div>
</div>

<div class="d-flex justify-content-center">
<div class="mw-auto mt-5">
<%= paginate @vacancies %>
</div>
</div>
</div>
</section>